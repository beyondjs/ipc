/************
Processor: ts
************/

import * as __beyond_dep_ns_0 from 'child_process';
import * as __beyond_dep_ns_1 from '@beyond-js/ipc/types';
// actions/index.ts
declare namespace ns_0 {
  /// <reference types="node" />
  import MainProcessHandler = ns_6.MainProcessHandler;
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  import IHandler = __beyond_dep_ns_1.IHandler;
  import Router = ns_2._default;
  /**
   * Handles action registration and execution in the main process.
   *
   * This class manages local action handlers and uses the router to:
   * - Dispatch actions to child processes.
   * - Register forked processes for action routing.
   *
   * Actions targeted at the main process are executed locally via `exec`.
   * Actions targeted at child processes are dispatched via `router`.
   */
  export class _default {
    #private;
    get router(): Router;
    constructor(main: MainProcessHandler);
    handle: (action: string, handler: IHandler) => Map<string, IHandler>;
    unhandle: (action: string) => boolean;
    off: (action: string) => boolean;
    has: (action: string) => boolean;
    /**
     * Register a forked child process to enable action routing.
     *
     * @param name - Unique name assigned to the forked process.
     * @param fork - The child process (fork) to register.
     */
    register(name: string, fork: ChildProcess): void;
    unregister(name: string): void;
    /**
     * Dispatch an action to a target child process.
     *
     * This sends a request to another process via the router and waits for its response.
     *
     * @param target - Name of the child process where the action should be executed.
     * @param action - Name of the action to execute.
     * @param params - Parameters to pass to the action.
     * @returns Promise resolving to the result of the action.
     */
    dispatch(target: string, action: string, ...params: any[]): Promise<any>;
    /**
     * Execute an action whose recipient is the main process
     *
     * @param action {string} The name of the action to execute
     * @param params {...any[]} The parameters to pass to the action
     * @returns any The response of the action
     */
    exec(action: string, ...params: any[]): Promise<any>;
    destroy(): void;
  }
}


// actions/router/child.ts
declare namespace ns_1 {
  /// <reference types="node" />
  import MainProcessHandler = ns_6.MainProcessHandler;
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  /**
   * Handles IPC messages received from a specific child process.
   *
   * This class is instantiated once per child process and is responsible for:
   * - Executing actions targeted at the main process.
   * - Forwarding actions to other child processes via a dispatcher.
   *
   * All incoming messages from the child process are listened to via `process.on('message')`,
   * and are filtered to detect `ipc.request` messages. Based on the `target`, the message is either:
   * - Executed locally (in the main process).
   * - Routed to another child process through the dispatchers registry in the main process.
   */
  export class _default {
    #private;
    get name(): string;
    constructor(main: MainProcessHandler, name: string, fork: ChildProcess);
    /**
     * Executes an action in the child process
     *
     * @param action {string} The name of the action to execute
     * @param params {...any[]} The parameters to pass to the action
     * @returns
     */
    dispatch(action: string, ...params: any[]): Promise<any>;
    destroy(): void;
  }
}


// actions/router/index.ts
declare namespace ns_2 {
  /// <reference types="node" />
  import MainProcessHandler = ns_6.MainProcessHandler;
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  export class _default {
    #private;
    constructor(main: MainProcessHandler);
    register(name: string, fork: ChildProcess): void;
    unregister(name: string): void;
    dispatch(target: string, action: string, ...params: any[]): Promise<any>;
    destroy(): void;
  }
}


// events/index.ts
declare namespace ns_3 {
  /// <reference types="node" />
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  import IListener = __beyond_dep_ns_1.IListener;
  export class _default {
    #private;
    constructor();
    /**
     * Register a forked child process to enable events routing.
     *
     * @param name - Unique name assigned to the forked process.
     * @param fork - The child process (fork) to register.
     */
    register(name: string, fork: ChildProcess): void;
    unregister(name: string): void;
    on(origin: string, event: string, listener: IListener): void;
    off(origin: string, event: string, listener: IListener): void;
    emit(event: string, message: any): void;
  }
}


// events/router/index.ts
declare namespace ns_4 {
  /// <reference types="node" />
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  import IListener = __beyond_dep_ns_1.IListener;
  export class _default {
    #private;
    constructor(listeners: Map<string, Set<IListener>>);
    emit(origin: string, event: string, message: any): void;
    register(name: string, fork: ChildProcess): void;
    unregister(name: string): void;
    destroy(): void;
  }
}


// events/router/origin.ts
declare namespace ns_5 {
  /// <reference types="node" />
  import Router = ns_4._default;
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  export class _default {
    #private;
    constructor(sources: Router, name: string, fork: ChildProcess);
    /**
     * Dispatch the event if the forked process is registered to it
     * The execution of this method is made by the router
     *
     * @param origin {string} The name of the process that is sending the event
     * @param event {string} The event name
     * @param message {*} The message to be sent
     */
    emit(origin: string, event: string, message: any): void;
    destroy(): void;
  }
}


// index.ts
declare namespace ns_6 {
  /// <reference types="node" />
  import IListener = __beyond_dep_ns_1.IListener;
  import IHandler = __beyond_dep_ns_1.IHandler;
  import IProcessHandler = __beyond_dep_ns_1.IProcessHandler;
  import ChildProcess = __beyond_dep_ns_0.ChildProcess;
  import Actions = ns_0._default;
  import Events = ns_3._default;
  export class MainProcessHandler implements IProcessHandler {
    #private;
    get id(): `${string}-${string}-${string}-${string}-${string}`;
    get actions(): Actions;
    handle: (action: string, handler: IHandler) => Map<string, IHandler>;
    removeHandler: (action: string) => boolean;
    constructor();
    get events(): Events;
    on(origin: string, event: string, listener: IListener): void;
    off(origin: string, event: string, listener: IListener): void;
    emit(event: string, data: any): void;
    /**
     * DEPRECATED: Use `emit` or `events.emit` instead.
     *
     * @param event {string} The name of the event to emit
     * @param data
     */
    notify(event: string, data: any): void;
    register(name: string, fork: ChildProcess): void;
    unregister(name: string): void;
    /**
     * Execute an IPC action
     *
     * @param target {string | undefined} The name of the target process
     * @param action {string} The name of the action being requested
     * @param params The parameters of the action
     */
    exec(target: string, action: string, ...params: any[]): Promise<any>;
    destroy(): void;
  }
  export const ipc: MainProcessHandler;
}


export import ipc = ns_6.ipc;

export declare const hmr: {on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };